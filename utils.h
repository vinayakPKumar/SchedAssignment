#ifndef UTILS_H
#define UTILS_H
#include <vector>
#include<string>
using namespace std;

extern uint16_t gcd(uint16_t a, uint16_t b);
extern uint16_t lcm (vector <uint16_t>&arr_au16, uint16_t n);
extern void delay(void);
#endif
